@model BookingMovieTickets.VIewModel.FilmVM
@using BookingMovieTickets
@using BookingMovieTickets.Models
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@using Microsoft.AspNetCore.Mvc.Rendering
@{
    ViewData["Title"] = "Quản trị";
    Layout = "_Layout";
}

@* 

 <style>
    .nav-item {
        position: relative;
    }

        .nav-item .nav-link {
            flex-grow: 1;
            margin-right: 1rem;
        }

        .nav-item .btn {
            margin-left: 5px;
        }

        .nav-item .btn,
        .nav-item .nav-link {
            display: flex;
            align-items: center;
        }

        .nav-item .btn,
        .nav-item .nav-link {
            line-height: normal; /* Ensure buttons and tabs are aligned vertically */
        }

        .nav-item .btn {
            height: auto; /* Ensure buttons do not stretch */
            padding: 0.375rem 0.75rem; /* Same padding as Bootstrap buttons */
        }

        .nav-item .nav-link {
            padding: 0.375rem 1rem; /* Adjust the padding of the tab */
        }

    .nav-tabs .nav-item.show .nav-link, .nav-tabs .nav-link.active {
        color: #495057;
        background-color: #e9ecef;
        border-color: #dee2e6 #dee2e6 #fff;
    }

    .nav-tabs .nav-link {
        border: 1px solid transparent;
        border-top-left-radius: 0.25rem;
        border-top-right-radius: 0.25rem;
    }

        .nav-tabs .nav-link:hover {
            border-color: #e9ecef #e9ecef #dee2e6;
        }
</style>

<script>
    $(document).ready(function () {
        $('#maimTab a').on('click', function (e) {
            e.preventDefault();
            $(this).tab('show');
        });
        $('#subTab a').on('click', function (e) {
            e.preventDefault();
            $(this).tab('show');
        });
    });
</script>
 *@

<div class="row">
    <!-- chart caard section start -->
    <div class="col-sm-6 col-xxl-3 col-lg-6">
        <div class="b-b-primary border-5 border-0 card o-hidden">
            <div class="custome-1-bg b-r-4 card-body">
                <div class="media align-items-center static-top-widget">
                    <div class="media-body p-0">
                        <span class="m-0">Tổng đơn</span>
                        <h4 class="mb-0 counter">@ViewBag.TotalOrdersCount</h4>
                    </div>
                    <div class="align-self-center text-center">
                        <i data-feather="database"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>


    <div class="col-sm-6 col-xxl-3 col-lg-6">
        <div class="b-b-success border-5 border-0 card o-hidden">
            <div class=" custome-4-bg b-r-4 card-body">
                <div class="media static-top-widget">
                    <div class="media-body p-0">
                        <span class="m-0">Tổng người dùng</span>
                        <h4 class="mb-0 counter">@ViewBag.CustomersCount</h4>
                    </div>

                    <div class="align-self-center text-center">
                        <i data-feather="users"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- chart caard section End -->
    <div class="col-sm-6 col-xxl-3 col-lg-6">
        <div class="b-b-success border-5 border-0 card o-hidden">
            <div class=" custome-4-bg b-r-4 card-body">
                <div class="media static-top-widget">
                    <div class="media-body p-0">
                        <span class="m-0">Doanh thu tạm tính</span>
                        <h4 class="mb-0 counter">@ViewBag.TotalRevenue$</h4>
                    </div>

                    <div class="align-self-center text-center">
                        <i data-feather="activity"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>